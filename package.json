{
  "name": "taggy-demo",
  "version": "0.0.1",
  "description": "",
  "keywords": [],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "taildev": "npm run dev && npx tailwindcss -i ./css/main.css -o ./dist/output.css --watch",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy-gh": "parcel build index.html --dist-dir docs --no-optimize --public-url ./"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.8.0",
    "@parcel/transformer-webmanifest": "^2.8.0",
    "autoprefixer": "^10.4.13",
    "parcel": "^2.8.0",
    "postcss": "^8.4.19",
    "tailwindcss": "^3.2.4"
  },
  "dependencies": {
    "prop-types": "^15.8.1"
  }
}
